/** 입력값 */
export type InputNumberValue = string | number | null | undefined;

/** Intl.NumberFormatOptions */
export type Options = Intl.NumberFormatOptions | undefined;

/** 지역정보 */
export type Locale = { code: string; currency: string };
